# Phase 0 验收报告

- 验收日期：2026-07-23
- 验收范围：开发机自动化检查、桌面窗口冒烟、Windows NSIS 打包及本机安装循环
- 开发分支：`phase-0/desktop-shell`
- 桌面实现提交：`c047f3f4b7a186321bfa83a9e2f439778990893c`
- 上游提交：`2d898a37c0341a2596f20b9de24a621a924f9804`
- 安装包：`FishTongue_0.0.1-phase.0_x64-setup.exe`
- 安装包大小：2,096,906 字节
- SHA-256：`B18FBF743C0597A2391688F0690536256D999D3997F6D4655C1936D5FBD0E27D`
- 代码签名：未签名
- 当前安装位置：`C:\Users\你的姓名\AppData\Local\FishTongue`
- 构建日志：[`logs/tauri-build.log`](logs/tauri-build.log)
- 网络检查：[`network-runtime-check.md`](network-runtime-check.md)
- 干净环境检查：[`clean-windows-readiness.md`](clean-windows-readiness.md)

## 结论

**当前结论：有条件通过开发机验证，尚未完成 Phase 0 最终验收。**

桌面程序、静态前端、编辑器和安装包已经做出来，发布版程序可直接启动，
Jest 与 Cypress 全部通过。本机首次安装、启动、卸载、重装和从开始菜单再次启动
也已通过。在宣布 Phase 0 完成前，还必须在不含开发工具的干净 Windows 环境复核。
当前不允许进入 Phase 1。

## 已实现内容

- Tauri 2 独立窗口和最小 `core:default` capability；
- Next.js `output: "export"` 静态前端；
- `/sc` 桌面路由及上游 CodeMirror/Lezer 编辑器复用；
- `UI → Service → Port ← Adapter` 边界和 Phase 2 引擎接入口；
- 真实的“尚未接入”引擎状态，不发送旧 Web API 请求，不伪造音变结果；
- Windows x64 NSIS 安装包。

## 自动化测试

| 命令                                                 | 结果 | 证据                                                        |
| ---------------------------------------------------- | ---- | ----------------------------------------------------------- |
| `npm run test-ci -- --runInBand`                     | 通过 | 14/14 套件、54/54 测试通过                                  |
| `npm run cypress-unit`                               | 通过 | 14/14 规格文件、67/67 测试通过                              |
| `npm run build:desktop`                              | 通过 | 删除旧 `out/` 后重新导出；`/` 与 `/sc` 均为静态页面         |
| 静态产物敏感引用扫描                                 | 通过 | 未发现 NextAuth、Neo4j、`/api/services` 或 Lexurgy 远程地址 |
| `cargo check --manifest-path .\src-tauri\Cargo.toml` | 通过 | Rust dev profile 检查完成                                   |
| `npm run tauri:build`                                | 通过 | 生成 2,096,906 字节 NSIS 安装包                             |

## 人工桌面验收

| 编号 | 操作                             | 实际结果                                               | 状态 |
| ---- | -------------------------------- | ------------------------------------------------------ | ---- |
| M-01 | 启动 `npm run tauri:dev`         | 独立 FishTongue 窗口正常显示                           | 通过 |
| M-02 | 查看编辑器                       | 行号、Lexurgy 语法颜色和示例规则正常显示               | 通过 |
| M-03 | 在真实窗口输入 `(`               | 字符计数从 124 变为 125                                | 通过 |
| M-04 | 使用 `Ctrl+Z`                    | 字符计数恢复为 124，新增字符消失                       | 通过 |
| M-05 | 查看引擎状态                     | 显示“尚未接入”，没有 Apply 或伪造结果                  | 通过 |
| M-06 | 直接启动 release 程序            | 发布版窗口正常显示，没有 Next.js 开发标记              | 通过 |
| M-07 | 关闭开发版与发布版               | 相关进程均正常结束，无已知残留                         | 通过 |
| M-08 | 括号自动补全与 `Ctrl+Y`          | Cypress 真实键盘事件验证通过                           | 通过 |
| M-09 | 100 行滚动与窗口缩放             | 输入 120 条规则、显示至第 240 行；最大化后编辑器仍可用 | 通过 |
| M-10 | 正式版运行时网络检查             | 启动、输入、撤销、关闭期间外部连接为 0                 | 通过 |
| M-11 | 本机 NSIS 安装、启动、卸载、重装 | 两次安装均成功；卸载清理完整；开始菜单再次启动成功     | 通过 |
| M-12 | 干净 Windows 安装循环            | 本机无 Sandbox、Hyper-V 或 Windows 虚拟机，需外部环境  | 阻断 |

## 与上游基线的差异

- 上游 Jest 基线为 11 套件、51 测试；当前为 14 套件、54 测试，全部通过。
- 上游生产构建会错误检查 `jest.setup.ts`；桌面生产配置只排除测试文件，业务源码仍参与检查。
- 上游 `/sc` 会调用远程 `/api/services`；桌面入口改用不可用适配器，等待 Phase 2
  接入本地 Kotlin Lexurgy Sidecar。
- 上游 Web 页面仍保留在源码历史中，但不进入桌面静态构建。

## 已知问题

| 编号 | 等级 | 问题                                                 | 处理决定                               |
| ---- | ---- | ---------------------------------------------------- | -------------------------------------- |
| K-01 | P1   | 尚无干净 Windows 安装、卸载、重装证据                | P0-09 必须完成后才能结束 Phase 0       |
| K-02 | P2   | 安装包未做代码签名，可能触发 SmartScreen             | Phase 0 如实记录；正式发布阶段处理签名 |
| K-03 | 上游 | 原始 `npm run build` 在 `jest.setup.ts` 类型检查失败 | 已保留上游记录；桌面构建隔离测试配置   |

## 本阶段不适用

| 项目                    | 本阶段状态 | 转为强制验收的阶段       |
| ----------------------- | ---------- | ------------------------ |
| 项目往返保存            | N/A        | Phase 1                  |
| SQLite 迁移、备份与恢复 | N/A        | Phase 1                  |
| Sidecar 启动与退出清理  | N/A        | Phase 2                  |
| 上游 Lexurgy 引擎测试   | N/A        | Phase 2 或首次修改引擎时 |

## 是否允许进入 Phase 1

**否。** 原因是 K-01 仍是 P1 阻断项。安装包已经生成不等于安装链路已经验收。
