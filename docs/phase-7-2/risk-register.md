# Phase 7.2 风险登记

> 状态：开发中。风险关闭必须有自动测试或 Windows 人工验收证据。

| 风险                       | 当前控制                                                                                                            | 验收证据                                            |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| 旧规则迁移丢失或重复       | v17 使用 `legacy_evolution_id` 唯一约束并保留旧表                                                                   | v16→v17 迁移测试；重复打开项目                      |
| 显示词形被误当作 IPA       | IPA 模式不回退到 `romanized`；缺失项标记 `not_run`                                                                  | 正常、缺失和临时 IPA 混合运行                       |
| 运行后来源或目标变化       | 保存来源/目标校验值，正式提交前重新计算                                                                             | 修改来源或目标后旧草稿阻断                          |
| Lexurgy 原始结果被人工覆盖 | Run 不可变；人工决定只写 Delivery                                                                                   | 历史记录与提交草稿对照                              |
| 新阶段或后代语言只写入一半 | 单个 SQLite 命令触发完整提交；外层项目操作失败恢复                                                                  | 四类目标失败回滚、撤销、重做                        |
| 后代语言引用来源内部编号   | 词条、Sense、语素和连接生成新编号并重映射                                                                           | 检查外键和来源删除后的完整性                        |
| 新音位被静默或不完整加入   | 未登记音是阻断问题；用户明确选择独立音位/音位变体，确认所属音位与自动预填的分布条件后，才随正式提交原子写入目标音系 | 两种角色、规则预填、修改、跳过、四类目标及撤销/重做 |
| 自动保存污染撤销栈         | 同一编辑会话使用合并键；边界操作结束合并                                                                            | 连续输入只撤销一次；回到原值无空记录                |
| 大词典导致界面卡顿         | 行使用浏览器内容可见性裁剪；正式验收使用 5,000 词项目                                                               | 5,000 词筛选、选择、取消运行                        |
| 深色模式或小窗口不可用     | 只使用锁定语义令牌；容器宽度折叠检查栏                                                                              | 浅/深主题与 1280×800 人工验收                       |
| 演化图形成第二套真相       | 图只读并从阶段、关系、Delivery 推导                                                                                 | 撤销/重做后图标记同步变化                           |
| 引擎旧产物不可用           | 完全复现要求版本与哈希一致；否则明确拒绝                                                                            | 版本匹配与不匹配各验收一次                          |
